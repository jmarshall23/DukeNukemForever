renderProg sunburstCasinoProgram
{
	vertex
	{
		gl_Position = TransformVertex($mvpMatrixX, $mvpMatrixY, $mvpMatrixZ, $mvpMatrixW);
		vertex_texcoord0 = attr_TexCoord0;
	}

	pixel
	{
		vec4 albedo = texture2D($albedotexture,vertex_texcoord0.st)  * 1.2;
		vec4 mask  = texture2D($customMask,vertex_texcoord0.st * vec2(30, 18));

		albedo.w = mask.r;		

		finalpixel_color = albedo;
		finalpixel_color4 = vec4(1.0, 0.0, 0.0, finalpixel_color.w);
	}
}